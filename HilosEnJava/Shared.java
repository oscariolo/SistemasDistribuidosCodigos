import java.util.LinkedList;
import java.util.Queue;

public class Shared {
    public static int count = 1; // Static variable to keep track of the number of instances
    public static Queue<Integer> queue = new LinkedList<>(); // Static variable to keep track of the number of instances
}
